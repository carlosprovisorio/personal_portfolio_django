# `from django.db import models` is importing the `models` module from the `django.db` package in
# Python. This allows you to use Django's built-in models to define the structure of your database
# tables when creating models in a Django application.
from django.db import models

# Create your models here.
